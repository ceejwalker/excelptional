{
    "version": "0.1.0",
    "configurations": [
        {
          "name": "Attach",
          "type": "chrome",
          "request": "attach",
          "port": 8080,
          "url": "http://localhost:8080",
          "webRoot": "${workspaceFolder}"
        },
        {
          "name": "Launch",
          "type": "chrome",
          "request": "launch",
          "port": 8080,
          "url": "http://localhost:8080",
          "webRoot": "${workspaceFolder}"
        }
    ]
}